# CLI-Student-Record-Management
A Python based CLI application for managing student records with CRUD operations, file based data persistence, input validation, exception handling, and a menu driven interface.
